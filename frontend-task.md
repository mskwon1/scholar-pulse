# Phase 4 Web App Task Board

## Setup & Infrastructure
- [ ] Initialize Next.js project (`frontend/`) with `export` configuration.
- [ ] Setup `shadcn/ui` with a dark premium theme.
- [ ] Integrate `@supabase/supabase-js`.
- [ ] Create `.github/workflows/deploy-s3.yml` for CI/CD to AWS S3.

## Authentication
- [ ] Build `/login` page with Email/Password standard login.
- [ ] Build `/signup` page with Email/Password account creation.
- [ ] Setup Middleware or Guard components for protected routes.

## Configuration Dashboard (`/dashboard`)
- [ ] Fetch current user's configuration from Supabase `user_config` table.
- [ ] Build UI to add/remove search keywords.
- [ ] Build UI to adjust SJR filters and citation limits.
- [ ] Save changes back to Supabase.
